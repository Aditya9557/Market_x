import 'package:flutter/material.dart';
// This is a placeholder — full implementation will be added when Flutter SDK is available.
class LoginScreen extends StatelessWidget {
  const LoginScreen({super.key});
  @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Login')));
}
